# root
type Query {
    # list all books
    books: [Book!]
    # list all authors
    authors: [Author!]
    # find author by id
    book(id: Int!): Book
}

# book
type Book {
    id: Int!
    name: String!
    author: Author!
}

# author
type Author {
    id: Int!
    name: String!
}